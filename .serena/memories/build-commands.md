# Build Commands for firmware project

## Full Build (Debug)
```bash
cube-cmake --build /Users/emmanuelcarjat/git/robot2Wheel/stm32Controller/firmware/build/Debug --target all --
```

## Notes
- Uses cube-cmake wrapper for STM32CubeMX CMake projects
- Build output is in `build/Debug/` directory
